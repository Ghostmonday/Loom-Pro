# Handoff — 2026-07-02, session paused for 5h usage reset

## State right now

`git status --porcelain` on `main`:

```bash
 M sandbox_frontend/shared/intent-forge-driver.js
 M sandbox_frontend/shared/shell.js
```

Everything else is committed and pushed. `git log --oneline -3`:

```bash
5d031f3 wip: interrogation-engine curriculum expansion (stalled, needs cleanup)
1ae86b3 docs: fix markdownlint violations in README
a23e4f8 test(dev): qualify smoke_locks verdict when Plan uses force override
```

`5d031f3` and `1ae86b3` are pushed to `origin/main`. The two modified files above are
**uncommitted** — real, verified fixes, just not committed yet (see below for why).

## Part 1 — Interrogation-engine curriculum (committed, pushed, not fully verified)

Expanded the interrogation curriculum from 7 to 16 required domains (tiered
DISCOVER → DEFINE → GUARD → OPERATE → PROVE order), added answer-quality
scoring, fixed two real bugs (`MAX_AUTO_RESOLUTION_PASSES` sized for the old
7-domain curriculum; DERIVE/DEFAULT/NOT_APPLICABLE resolutions never carried
a `domain` key, causing miscredited auto-resolutions and an infinite-loop risk).

Also fixed a readiness-gate bug found this session: in
`aoc_supervisor/aoc_supervisor/reasoning_provider.py`, `analyze()` used to
early-return on `readiness.ready_to_finalize`, which let FINALIZE fire before
low-risk rescue-draft items (deliberately risk=0.5, auto-DEFAULT) were ever
resolved. Changed the gate to `if not uncertainties:` so every remaining item
is resolved before the engine calls it done.

**Verified:** `tests/test_interrogation_curriculum.py` passes 15/15.

**NOT verified — and now actively concerning.** I started
`python3 -m pytest -q --no-cov` in the background early this session (task
id `b7mmgaq2f`). It ran 75+ minutes of CPU time, then the background task
wrapper reported "completed (exit code 0)" — but that exit code is from
`tail -60` in the pipe (`pytest ... | tail -60`), not from pytest itself, so
it's meaningless. The captured output is only 3 lines and cuts off mid
progress-bar with **no trailing newline and no final summary**:

```
........................................................................ [  7%]
........................................................................ [ 15%]
................................................F....................... [ 22%]
.............................................
```

There is **at least one confirmed failure (`F`)** around the 22% mark
(roughly test #118 of the collected set), and the run never produced a
finish summary — it looks like it was killed/interrupted rather than
finishing normally. **Do not trust the earlier "14/15 → 15/15" framing as
proof the whole suite is healthy.** A **second** full-suite run also
appears to be in flight (pid 4101813, started ~06:51, ~25 min CPU so far) —
I did not start this second one; it may be the cleanup agent or a
duplicate from elsewhere. On resume: kill both, run one clean
`python3 -m pytest -q --no-cov -x` (stop at first failure) to identify the
actual failing test near 22%, and get a real pass/fail verdict before
committing anything else on top of the curriculum change.

## Part 2 — Repo cleanup (paused, handed to a separate agent)

You asked me to hand this off to another agent so it wouldn't collide with
my work, and told that agent to leave the frontend alone. Before handing
off, I found the original cleanup briefing was **wrong on item 1**: it
claimed `sandbox_frontend/*.html` top-level files (`blueprint-ratification.html`,
`claims-ledger.html`, etc.) were dead since the initial commit. They are not —
`aoc_supervisor/aoc_supervisor/routers/static_ui.py` serves them live via
`GET /sandbox/{page_name}` and `GET /structural-canvas`, resolved through
`repo_paths.SANDBOX_PAGES`. Nothing internally links to those routes, but
deleting the files would have broken working (if unlinked) endpoints. Do not
let a cleanup pass delete them without confirming that dict / those routes
are truly unused end-to-end.

Item 2 (`loom-frontend-base` vs `frontend-formation-complete`) — found
`docs/reference/canonical-authority.registry.json`, which already declares
`frontend-formation-complete` an `inactive_duplicate_root` and
`loom-frontend-base/frontend-formation` the canonical build source. I was
independently verifying this (checking for a build script, timestamps,
whether anything actually imports `frontend-formation-complete`) when this
got paused — `grep` showed no non-test Python imports it, but I hadn't yet
confirmed the registry's claim is *itself* accurate (i.e. that
`frontend-formation-complete` really is regeneratable output and not
independently-edited content) before you told me to leave it to another
agent. Since that agent was told to leave the frontend alone, this item is
likely still open — check with whoever ran that cleanup pass.

Item 3 (`dist/provisional-filing/`) — untouched, per original instructions
(possible patent filing evidence trail, needs explicit confirmation before
touching).

## Part 3 — Real UI bugs found and fixed (uncommitted)

Big correction from earlier in the session: `ui/blueprint-ui.json`
(`"surface": "neural-draft-internal-ui"`) describes a **different, unbuilt**
feature — a swarm/grid orchestration control panel — not what
`scripts/dev/smoke_locks.py` tests. Its declared API endpoints
(`/api/v1/grid/*`, `/api/v1/orchestrate/*`) don't overlap with
`smoke_locks.py`'s `/api/v1/intent-forge/*` calls at all. Only a task spec
exists for it (`docs/codex-tasks/task-neural-draft-ui.md`) — no HTML/JS.

The **real** UI `smoke_locks.py` mirrors is `sandbox_frontend/workspaces/intent-forge.html`

+ `sandbox_frontend/shared/intent-forge-driver.js`, loaded into an SPA shell
by `sandbox_frontend/shared/shell.js`, served live. Reachable at
`http://127.0.0.1:8080/#intent-forge` (hash-routed, and the default landing
workspace). A `loom` server was already running on port 8080 (pid 3450658,
started before this session — not mine, I don't have its API key).

You opened it and reported it "never gets past this screen." Root cause
found and confirmed live:

1. **`intent-forge-driver.js` reads `localStorage.getItem("loom.api_key")`
   for every request, but nothing anywhere in `sandbox_frontend/` ever
   wrote that key.** Every request 401s
   (`curl` reproduction: `{"detail":"Invalid or missing API key"}`).
   The UI fails silently — no error surfaced to the user.
2. **The "API Ready" badge on the Build screen was hardcoded static HTML**
   (`sandbox_frontend/workspaces/intent-forge.html:17`), never actually
   checking anything — so the UI *claimed* readiness while broken.

**Fixes applied (uncommitted, verified live via headless Chrome
screenshots — before/after in
`/tmp/claude-1000/-home-ghostmonday/62c730cb-6a70-4a9f-be5c-ca5686adc3a5/scratchpad/{before_fix,after_fix}.png`,
may not survive past this session, worth re-screenshotting on resume):**

+ `sandbox_frontend/shared/shell.js`: wired the previously-dead `#shell-settings`
  gear button to a new `openApiKeyPanel()` — a small modal to paste/save the
  API key into `localStorage`. Added a red dot indicator on the gear when no
  key is set.
+ `sandbox_frontend/shared/intent-forge-driver.js`: added `updateApiPill()`,
  called on workspace load and exposed on `window.LoomIntentForgeDriver` so
  `shell.js` can refresh it after a key is saved. The pill now reads
  `localStorage` for real: red "API key required" when missing (confirmed
  in screenshot), green "API Ready" once set.

**Verified:** static rendering (screenshots) confirms both indicators now
honestly reflect key-presence state.

**NOT verified:** the actual click → modal-opens → save → pill-updates
interaction. No Playwright/Puppeteer installed in this environment
(`pip3 install playwright` untested — network access to install packages
wasn't confirmed working). Only static `google-chrome --headless=new
--screenshot=...` was available, which can't script clicks. Two ways to
close this on resume:

1. Manually click the gear on the live UI, paste a real key, confirm Build
   progresses.
2. Install Playwright and script the full click-through — this was the
   direction we were heading (a "human-level" visual smoke test: DOM-state
   assertions + screenshots at each of the 5 phase gates in
   `scripts/dev/smoke_locks.py`, deterministic, no flaky vision calls unless
   engineered with structured output / retries / temperature 0 as discussed).

## Next steps, in priority order

1. **Resolve the pytest hang.** Kill both background full-suite runs, run
   one clean `python3 -m pytest -q --no-cov`, and actually watch it finish
   before trusting anything about test status again.
2. **Manually verify the API-key modal interaction** on the live UI (or
   install Playwright and script it), then commit the two `sandbox_frontend`
   fixes with a clear message (these are real product bugs, not WIP —
   worth their own commit, separate from the interrogation-engine WIP
   already pushed).
3. **Check in with whoever is running the repo-cleanup agent** — item 2
   (`frontend-formation-complete`) was left genuinely unresolved, and the
   "dead top-level HTML files" premise in the original briefing was already
   proven wrong once; don't let a second pass repeat that mistake.
4. Once the full suite is green and the intent-forge UI genuinely reaches
   FINALIZED/HANDED_OFF end to end (both via `smoke_locks.py` and by hand
   in the browser), build the Playwright-based visual smoke harness against
   `sandbox_frontend/workspaces/intent-forge.html` — screenshot + DOM
   assertion at each phase gate, with the API-key bug as its first proven
   regression case.

## House rules (unchanged)

No Co-Authored-By / AI-attribution lines in commits. Single branch: `main`.
Verify live before declaring anything fixed — this session, "verified" means
either a passing test run I actually watched finish, or a screenshot/curl
reproduction, not a diff read.
